NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# 絕不加 NEXT_PUBLIC_ 前綴
SUPABASE_SERVICE_ROLE_KEY=

# Vercel Cron 的憑證。cron 會帶 Authorization: Bearer <這個值>。
# 沒設的話 /api/cron/ingest 一律拒絕——不要「沒設就放行」。
CRON_SECRET=
